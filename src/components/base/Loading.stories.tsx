import { Loading } from './Loading'
import { ComponentMeta } from '@storybook/react'

export default {
  title: 'Loading',
  component: Loading
} as ComponentMeta<typeof Loading>

export const Example = <Loading></Loading>
